import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:levaai1/app/pedidos/pedido_repository/pedido_repository_page.dart';

void main() {
  testWidgets('PedidoRepositoryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PedidoRepositoryPage(title: 'PedidoRepository')));
    //  final titleFinder = find.text('PedidoRepository');
    //  expect(titleFinder, findsOneWidget);
  });
}
