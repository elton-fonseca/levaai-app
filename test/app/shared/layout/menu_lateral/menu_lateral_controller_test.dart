import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:levaai1/app/shared/layout/menu_lateral/menu_lateral_controller.dart';
import 'package:levaai1/app/app_module.dart';

void main() {
  initModule(AppModule());
  // MenuLateralController menulateral;
  //
  setUp(() {
    //     menulateral = AppModule.to.get<MenuLateralController>();
  });

  group('MenuLateralController Test', () {
    //   test("First Test", () {
    //     expect(menulateral, isInstanceOf<MenuLateralController>());
    //   });

    //   test("Set Value", () {
    //     expect(menulateral.value, equals(0));
    //     menulateral.increment();
    //     expect(menulateral.value, equals(1));
    //   });
  });
}
