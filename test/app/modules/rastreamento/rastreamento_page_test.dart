import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RastreamentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget
    //(RastreamentoPage(title: 'Rastreamento')));
    //  final titleFinder = find.text('Rastreamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
