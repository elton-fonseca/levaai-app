import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:levaai1/app/modules/pedido/pedido_controller.dart';
import 'package:levaai1/app/modules/pedido/pedido_module.dart';

void main() {
  initModule(PedidoModule());
  // PedidoController pedido;
  //
  setUp(() {
    //     pedido = PedidoModule.to.get<PedidoController>();
  });

  group('PedidoController Test', () {
    //   test("First Test", () {
    //     expect(pedido, isInstanceOf<PedidoController>());
    //   });

    //   test("Set Value", () {
    //     expect(pedido.value, equals(0));
    //     pedido.increment();
    //     expect(pedido.value, equals(1));
    //   });
  });
}
