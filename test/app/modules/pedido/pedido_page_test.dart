import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:levaai1/app/modules/pedido/pages/pedido_listar_page.dart';

void main() {
  testWidgets('PedidoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PedidoPage(title: 'Pedido')));
    //  final titleFinder = find.text('Pedido');
    //  expect(titleFinder, findsOneWidget);
  });
}
